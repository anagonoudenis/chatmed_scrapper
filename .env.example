# Example environment variables for ChatMed Scraper
# Copy this file to .env and fill in your values
# Note: We use config.toml by default, but you can use .env if preferred

# DeepSeek API Configuration (for autonomous agent)
DEEPSEEK_API_KEY=sk-your-api-key-here

# Optional: PubMed API Key (for faster rate limits)
PUBMED_API_KEY=

# Optional: Custom API endpoints
DEEPSEEK_API_URL=https://api.deepseek.com/v1/chat/completions
